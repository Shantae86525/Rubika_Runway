rubpy
telethon==1.36.0
python-dotenv==1.0.1
# Optional but recommended for faster Telethon encryption:
# cryptg==0.4.0

# ---- Worker subsystem (master + worker) ----
# Secret encryption for stored SSH credentials / tokens.
cryptography>=42.0.0
# Master <-> worker provisioning and secure SSH tunnel.
asyncssh>=2.14.0
# HTTP client (master) + server (worker API).
httpx>=0.27.0
fastapi>=0.110.0
uvicorn>=0.29.0
# Timezone data for Asia/Tehran timestamps on minimal/foreign servers.
tzdata>=2024.1
